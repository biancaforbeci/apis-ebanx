version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - zip -r deploy.zip dist package.json package-lock.json Procfile node_modules

artifacts:
  files:
    - deploy.zip